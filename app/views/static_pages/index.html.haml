= render 'header'
%section#intro
  .container.white-color
    %h1 Socrates is a real-time classroom analytics platform.
    = link_to 'Log In', create_session_path, :class => "btn btn-outline btn-lg"
    -# %a.btn.btn-outline.btn-lg{:href => "getting-started#download", :onclick => "ga('send', 'event', 'Jumbotron actions', 'Download', 'Download 3.3.2');"} Log In

%section#first-pane{:style => "padding-top:20px;"}
  .container
    %section.content.vertical-align.media-then-text
      %article.half.text-content
        %h2 Track how much you know about a topic in class.
        %p
          Clever &amp; fun to use. Pennies does all the hard work for you by telling you how much you have to spend today. If you spend less, no worries, Pennies will give you more to spend tomorrow.
      %article.half.media-content
        %img.reduced-width{:alt => "A simple way to track your finances", :src => "/assets/iphone-slanted-super-simple.png", :style => "max-width:333px;", :title => "Pennies app super simple"}
-# .home-teaser-image
-# 	.logo-wrapper
-# 		%img{:src => "/assets/socrates_black.png"}
-# 	.home-app-description Socrates is a real-time learning analytics platform for teachers and students.
-# 	%p{:style => "text-align: center; margin-top:20px;"}
-# 		- if session[:user_id] && UTEPSSO.authenticated?(cookies[:UTEP_SE], cookies[:UTEP_SA])
-# 			- @current_user = current_user
-# 			= link_to "#{@current_user.name.split(' ').first}, Sign Out", signout_path, id: "sign_out", :class => "custom-home-button"
-# 		- else
-# 			= link_to 'Sign-In', "https://adminapps.utep.edu/sso?redirectURL=#{create_session_url}", :class => "custom-home-button"
-#
-# .intro-text-wrapper.spacing-from-header
-# 	.container.info{:style => "text-align:center;"}
-# 		%h2 Get real-time information on how the class is doing.
-# 		%img{:src => "/assets/rubymotion-factory.giindex.html.haml
