.dashboard-wrapper
	.dashboard-menu-wrapper
		= render './layouts/page_title'
	.container.page-content-wrapper
		- @sections.each do |section|
			= link_to section, {:style => "text-decoration:none;"} do
				.subject-name
					= section.name
	= link_to 'New Section', new_section_path
