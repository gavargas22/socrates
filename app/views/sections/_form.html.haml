= form_for(@section) do |f|
	-# - if @section.errors.any?
	-#   #error_explanation
	-#     %h2
	-#       = pluralize(@section.errors.count, "error")
	-#         prohibited this section from being saved:
	-#
	-#     %ul
	-#       - @section.errors.full_messages.each do |message|
	-#         %li= message
	.field
		= f.label :name
		%br/
		= f.text_field :name

	.field
		= f.label :department
		%br/
		= f.text_field :department

	.field
		%p
		- @subject_id = params[:subject_id]
		#{@subject_id}
		= hidden_field(:subject_id, @subject_id)
		%br
		-# = f.text_field :subject_id
		-# = f.fields_for :subject do |s|
		-# 	= s.collection_select :subject_ids, all_subjects, :id, :name, {}, {:multiple => true}

	-#   = f.fields_for :subject do |s|
	-#     = s.collection_select :subject, Provider.order(:name),:id,:name, include_blank: true
	-#
	-# .field
	-#   = f.label :subject
	-#   %br/
	-#   = f.text_field :subject, section.subject

	.field
		= f.label :number
		%br/
		= f.text_field :number

	.actions
		= f.submit
